# library(covr)
# # remove: --as-cran and rebuild
# coverage <- covr::package_coverage(path="/Users/mjk2/Dropbox (Personal)/R_packages/parseRPDR/", quiet = FALSE)
# coverage
# report(coverage)
# codecov(token = "YOUR_TOKEN_HERE")
